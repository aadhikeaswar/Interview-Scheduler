import React from "react";
class Template extends React.Component{
    constructor(props){
        super(props);
        //Props are the data and functions passed to a component from its parent component
        this.state ={
        //The variables declared in the state object are accessible through the entire component    
        }
        
    }
    render(){
        //The return function returns a HTML object which is used to build the GUI
        return(
         <div> 
             
         </div>
        );
    }

}

export default Template;